# === Django 核心（注意：urls.py 使用了 django.conf.urls.url，需 Django < 4.0）===
Django==3.2.25
djangorestframework==3.14.0
django-filter==21.1
django-cors-headers==3.13.0
django-celery-beat==2.5.0
django-redis==5.2.0
gunicorn==21.2.0

# === 数据库驱动 ===
mysqlclient==2.1.1
# 若 MySQL 客户端未安装，可改用纯 Python 驱动：
# PyMySQL==1.1.0

# === Celery ===
celery==5.3.6
redis==4.5.4

# === SNMP 采集 ===
pysnmp==4.4.12
# easysnmp 依赖系统 net-snmp 开发库，若环境未安装可注释掉
easysnmp==0.2.5

# === SSH / 网络设备采集 ===
paramiko==3.4.0
netmiko==4.3.0

# === IPMI 采集 ===
pyghmi==1.5.35

# === 时间序列数据库（可选）===
# influxdb-client==1.38.0

# === 报表导出 ===
openpyxl==3.1.2
reportlab==4.1.0
Jinja2==3.1.3

# === 工具库 ===
python-dotenv==1.0.1
requests==2.31.0
pycryptodome==3.20.0
python-json-logger==2.0.7
netaddr==1.2.1
pyyaml==6.0.1
