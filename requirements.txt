# === 强制使用国内镜像（覆盖平台默认的已停服豆瓣源 pypi.douban.com；若清华不通可换 https://mirrors.aliyun.com/pypi/simple/）===
--index-url https://pypi.tuna.tsinghua.edu.cn/simple/

# === Django 核心（已升级到 4.2 LTS 以兼容 Python 3.12；urls.py 改用 re_path 替代已移除的 url()）===
Django>=4.2,<5.0
# 注意：不要在此声明 uwsgi！
# paas-agent 部署时先用豆瓣源执行 pip install uwsgi==2.0.28（系统已装 → already satisfied 通过）；
# 若 venv 里装了别的 uwsgi 版本(如 2.0.27)，该步骤会因版本不匹配去已停服的豆瓣源查 2.0.28 → 部署失败。
# venv/bin/uwsgi 由部署后软链系统 uwsgi 提供（ln -sf /usr/local/bin/uwsgi Envs/<app>/bin/uwsgi），
# wsgi.py 已做 venv site-packages 注入兼容。
# blueapps 是 OpsAny/蓝鲸 PaaS 基础框架；该 OpsAny 实例未在应用 venv 自动注入，
# 需在此显式声明由 pip 安装，否则 migrate 时报 ModuleNotFoundError: No module named 'blueapps'
blueapps>=4.0,<5.0
djangorestframework>=3.15,<4.0
django-filter>=23.5,<25.0
django-cors-headers>=4.0,<5.0
django-celery-beat>=2.6,<3.0
django-redis>=5.4,<6.0
gunicorn==21.2.0

# === 数据库驱动 ===
# mysqlclient 需本地有 MySQL 开发库(编译)，离线构建机易失败；改用纯 Python 驱动
PyMySQL==1.1.0
# 若构建机已配 MySQL 客户端开发库，可改回 mysqlclient==2.1.1
# 凭据加密字段（CMDB 凭据模型用 django_cryptography 加密存储；缺失则 migrate 报
# ModuleNotFoundError: No module named 'django_cryptography'）
django-cryptography==1.1

# === Celery ===
celery==5.3.6
redis==4.5.4

# === SNMP 采集 ===
pysnmp==4.4.12
# easysnmp 依赖系统 net-snmp 开发库（离线构建机无，必失败），已移除；SNMP 主采集走 pysnmp


# === SSH / 网络设备采集 ===
paramiko==3.4.0
netmiko==4.3.0

# === IPMI 采集 ===
pyghmi==1.5.35

# === 时间序列数据库（可选）===
# influxdb-client==1.38.0

# === 报表导出 ===
openpyxl==3.1.2
reportlab==4.1.0
Jinja2==3.1.3

# === 工具库 ===
python-dotenv==1.0.1
requests==2.31.0
pycryptodome==3.20.0
python-json-logger==2.0.7
netaddr==1.2.1
pyyaml==6.0.1
